{
  "name": "bridge",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --parallel --filter @atlascrew/bridge --filter @bridge/web run dev",
    "build": "pnpm -r run build",
    "type-check": "pnpm -r run type-check"
  }
}
